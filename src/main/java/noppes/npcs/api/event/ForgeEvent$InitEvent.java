package noppes.npcs.api.event;

import net.minecraftforge.fml.common.eventhandler.Event;

public class ForgeEvent$InitEvent extends ForgeEvent {
   public ForgeEvent$InitEvent() {
      super((Event)null);
   }
}
